
TMBStrucModel = function(
      before = list()
    , during = list()
    , after = list()
    , state_vector = list()
    , other_vectors = list()
    #, must_save = character()
    #, must_not_save = character()
    #, sim_exprs = character()
    #, state_update = "no"
  ) {
  
}
  
#' Structured TMB Model Specification
#' 
#' 
#' 
#' @export
mp_tmb_struc_model = TMBStrucModel

