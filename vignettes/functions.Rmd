---
title: "Mathematical Functions in McMasterPandemic"
output: 
  rmarkdown::html_vignette:
    toc: true
vignette: >
  %\VignetteIndexEntry{Mathematical Functions in McMasterPandemic}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
library(macpan2)
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```


## Binary Operators

[Binary operators](https://canmod.net/misc/elementwise_binary_operators)

`+`, `-`, `*`, `/`, `^`

## Parentheses

## Vector and Matrix Construction

* `c`
* `matrix`
* `sparse_matrix(x, v, m)`
    * `x` -- A matrix
    * `v` -- A column vector of values to add to elements of x
    * `m` -- A matrix of integers

```{cpp eval = FALSE}
# 1. Fill x with all zeros.
x[,] = 0

# 2. Add elements in v to elements of x, according to the indices in m.
for (int k=0; i<n; i++)
  x[m[k,0], m[k,1]] += v[m[k,2], 0]

# 3. Return x.
x
```
