# Empty Trajectory

Output of
[`mp_trajectory`](https://canmod.github.io/macpan2/reference/mp_trajectory.md)
if nothing is simulated.

## Usage

``` r
empty_trajectory
```

## Format

A data frame with zero rows and the following columns: `matrix`, `time`,
`row`, `col`, `value`.
