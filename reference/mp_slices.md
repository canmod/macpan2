# Slice an index

Slice an index

## Usage

``` r
mp_slices(index, unpack = c("no", "maybe", "yes"))
```

## Arguments

- index:

  Index to slice up.

- unpack:

  Place factors in the global environment?
