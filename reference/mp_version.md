# Version of `macpan2`

Version of `macpan2`

## Usage

``` r
mp_version(model)
```

## Arguments

- model:

  `macpan2` model object.

## Value

Object of type
[`package_version`](https://rdrr.io/r/base/numeric_version.html) giving
the version of `macpan2` that produced `model`.
