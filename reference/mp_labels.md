# Index Labels

Return a character vector of labels for each row of an index (or a
ledger?? FIXME: what does this mean for ledgers??).

## Usage

``` r
mp_labels(x, labelling_column_names)
```

## Arguments

- x:

  Object

- labelling_column_names:

  What index columns should be used for generating the labels. If
  missing then defaults will be used. (FIXME: clarify how the defaults
  are used.)
