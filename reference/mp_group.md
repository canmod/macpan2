# Group an Index

Create a new index with fewer columns to create names for an aggregated
vector that is labelled by the input index.

## Usage

``` r
mp_group(index, by)
```

## Arguments

- index:

  Index to group rows.

- by:

  Column set label to group by.
