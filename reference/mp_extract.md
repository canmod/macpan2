# Extract Index

Extract the index for a particular dimension in a ledger from a ledger
or from an object containing one or more ledgers.

## Usage

``` r
mp_extract(x, dimension_name)
```

## Arguments

- x:

  Object

- dimension_name:

  Name of a dimension used in a ledger.
